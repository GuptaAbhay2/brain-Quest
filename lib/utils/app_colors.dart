import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const Color background = Color(0xFF0F1123);
  static const Color surface = Color(0xFF1A1D3A);
  static const Color surfaceLight = Color(0xFF242850);

  // Brand
  static const Color primary = Color(0xFF7C4DFF);
  static const Color secondary = Color(0xFF00E5FF);

  // Feedback
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFFF5252);
  static const Color star = Color(0xFFFFC107);

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB8BCE0);
  static const Color textMuted = Color(0xFF6B7099);

  // Level circle states
  static const Color lockedLevel = Color(0xFF2A2D52);
  static const Color completedLevel = Color(0xFF7C4DFF);
  static const Color currentLevelGlow = Color(0xFF00E5FF);
}